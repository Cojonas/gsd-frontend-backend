language: node_js
node_js: 
  - "8"
script:
  - "cd server && npm i"
  - "npm test"
  - "npm build"
  - "cd ../client && npm i && npm run build"
  - "cd ../server/public && mkdir appfiles && cp -r ../../client/build/* ./appfiles && ls"
on: 
  branch: master
